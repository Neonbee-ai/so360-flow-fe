name: Preview Deploy

on:
  push:
    branches: [preview]

jobs:
  deploy:
    runs-on: [self-hosted, macOS, ARM64]
    timeout-minutes: 30
    steps:
      - name: Deploy to preview
        run: ~/SO360/preview-deploy.sh flow-fe so360-flow/so360-flow-fe build
